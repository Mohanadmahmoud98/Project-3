---
- name: "copy files"
  become: yes
  copy: 
    src: ../../../../backendartifact.tar.gz
    dest: ../ubuntu

- name: "start"
  become: yes
  shell: |
    tar -xzvf backendartifact.tar.gz -C backends
    cd backends
    pm2 start npm --run start